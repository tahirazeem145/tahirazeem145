<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 500" width="100%" height="100%" preserveAspectRatio="xMidYMid meet">
  <defs>
    <!-- Embedded & Google Fonts -->
    <style>
      @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@400;600;700;800;900&amp;family=Space+Grotesk:wght@400;500;600;700&amp;family=JetBrains+Mono:ital,wght@0,400;0,500;0,700;1,400&amp;display=swap');

      /* Base Typography */
      .font-display {
        font-family: 'Outfit', 'Space Grotesk', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
      }
      .font-mono {
        font-family: 'JetBrains Mono', 'Fira Code', 'Cascadia Code', 'Courier New', monospace;
      }

      /* Opacity-Safe Entrance Animations */
      @keyframes fadeIn {
        0% { opacity: 0; }
        100% { opacity: 1; }
      }

      @keyframes titleReveal {
        0% { opacity: 0; filter: blur(8px); }
        60% { opacity: 0.8; filter: blur(2px); }
        100% { opacity: 1; filter: blur(0px); }
      }

      @keyframes vibeGlowPulse {
        0%, 100% {
          filter: drop-shadow(0 0 6px rgba(0, 240, 255, 0.45)) drop-shadow(0 0 20px rgba(168, 85, 247, 0.35));
        }
        50% {
          filter: drop-shadow(0 0 16px rgba(0, 240, 255, 0.85)) drop-shadow(0 0 35px rgba(168, 85, 247, 0.6));
        }
      }

      @keyframes ambientAura {
        0%, 100% { opacity: 0.45; transform: scale(1); }
        50% { opacity: 0.8; transform: scale(1.08); }
      }

      @keyframes scanSweep {
        0% { transform: translateY(-100px); opacity: 0; }
        25% { opacity: 0.35; }
        75% { opacity: 0.35; }
        100% { transform: translateY(520px); opacity: 0; }
      }

      @keyframes particleFloatA {
        0% { transform: translateY(0) translateX(0); opacity: 0.2; }
        50% { transform: translateY(-50px) translateX(10px); opacity: 0.9; }
        100% { transform: translateY(-100px) translateX(-6px); opacity: 0.1; }
      }

      @keyframes particleFloatB {
        0% { transform: translateY(0) translateX(0); opacity: 0.25; }
        50% { transform: translateY(-65px) translateX(-12px); opacity: 0.85; }
        100% { transform: translateY(-120px) translateX(8px); opacity: 0.1; }
      }

      @keyframes constellationPulse {
        0%, 100% { opacity: 0.3; stroke-dashoffset: 0; }
        50% { opacity: 0.7; stroke-dashoffset: 20; }
      }

      @keyframes orbitRotate {
        0% { transform: rotate(0deg); }
        100% { transform: rotate(360deg); }
      }

      @keyframes cursorBlink {
        0%, 49% { opacity: 1; }
        50%, 100% { opacity: 0; }
      }

      @keyframes terminalTypeA {
        0%, 15% { opacity: 0; }
        25%, 100% { opacity: 1; }
      }
      @keyframes terminalTypeB {
        0%, 35% { opacity: 0; }
        45%, 100% { opacity: 1; }
      }
      @keyframes terminalTypeC {
        0%, 55% { opacity: 0; }
        65%, 100% { opacity: 1; }
      }

      /* Animation Layer Classes */
      .anim-bg { animation: fadeIn 1.2s ease-out forwards; }
      .anim-grid { animation: fadeIn 1.8s ease-out 0.2s both; }
      
      .anim-nodes { animation: fadeIn 1.6s ease-out 0.6s both; }
      .anim-constellation { animation: constellationPulse 5s ease-in-out infinite; }
      
      .anim-orbit {
        transform-origin: 600px 225px;
        animation: orbitRotate 40s linear infinite;
      }

      .anim-header-hud { animation: fadeIn 0.8s ease-out 0.4s both; }
      .anim-footer-hud { animation: fadeIn 0.8s ease-out 0.6s both; }

      .anim-pretitle { animation: fadeIn 0.9s ease-out 1.2s both; }

      .anim-title {
        animation: titleReveal 1.2s ease-out 1.8s both,
                   vibeGlowPulse 4s ease-in-out infinite 3.2s;
      }

      .anim-badge { animation: fadeIn 0.9s ease-out 2.8s both; }
      .anim-subtitle { animation: fadeIn 0.9s ease-out 3.4s both; }
      .anim-tagline { animation: fadeIn 0.9s ease-out 4.0s both; }
      .anim-actions { animation: fadeIn 0.9s ease-out 4.4s both; }

      .anim-terminal { animation: fadeIn 1s ease-out 1.0s both; }

      .anim-aura {
        transform-origin: 600px 230px;
        animation: ambientAura 7s ease-in-out infinite alternate;
      }

      .scanline {
        animation: scanSweep 7s cubic-bezier(0.4, 0, 0.2, 1) infinite;
      }

      /* Particles */
      .p1 { animation: particleFloatA 5.2s ease-in-out infinite; }
      .p2 { animation: particleFloatB 6.8s ease-in-out infinite 0.7s; }
      .p3 { animation: particleFloatA 8.4s ease-in-out infinite 1.4s; }
      .p4 { animation: particleFloatB 6.1s ease-in-out infinite 2.2s; }
      .p5 { animation: particleFloatA 7.5s ease-in-out infinite 3.1s; }
      .p6 { animation: particleFloatB 9.2s ease-in-out infinite 1.9s; }

      .cursor { animation: cursorBlink 0.85s infinite; }
      .t-line-1 { animation: terminalTypeA 3.5s ease-out 1.2s both; }
      .t-line-2 { animation: terminalTypeB 3.5s ease-out 1.2s both; }
      .t-line-3 { animation: terminalTypeC 3.5s ease-out 1.2s both; }

      .btn-hover {
        cursor: pointer;
        transition: filter 0.25s ease, opacity 0.25s ease;
      }
      .btn-hover:hover {
        filter: drop-shadow(0 0 12px rgba(0, 240, 255, 0.75));
      }
    </style>

    <!-- Gradients -->
    <radialGradient id="bgGlow" cx="50%" cy="50%" r="65%">
      <stop offset="0%" stop-color="#0c1224" stop-opacity="0.95" />
      <stop offset="45%" stop-color="#070a14" stop-opacity="0.98" />
      <stop offset="100%" stop-color="#020307" stop-opacity="1" />
    </radialGradient>

    <!-- Cyan Radial Glow -->
    <radialGradient id="cyanAura" cx="40%" cy="46%" r="42%">
      <stop offset="0%" stop-color="#00f0ff" stop-opacity="0.2" />
      <stop offset="50%" stop-color="#00d2ff" stop-opacity="0.06" />
      <stop offset="100%" stop-color="#000000" stop-opacity="0" />
    </radialGradient>

    <!-- Violet Radial Glow -->
    <radialGradient id="violetAura" cx="60%" cy="50%" r="42%">
      <stop offset="0%" stop-color="#a855f7" stop-opacity="0.22" />
      <stop offset="60%" stop-color="#7928ca" stop-opacity="0.05" />
      <stop offset="100%" stop-color="#000000" stop-opacity="0" />
    </radialGradient>

    <!-- Core Center Orb -->
    <radialGradient id="centerCore" cx="50%" cy="48%" r="35%">
      <stop offset="0%" stop-color="#00f0ff" stop-opacity="0.28" />
      <stop offset="35%" stop-color="#a855f7" stop-opacity="0.16" />
      <stop offset="70%" stop-color="#050814" stop-opacity="0" />
    </radialGradient>

    <!-- Typography Gradient -->
    <linearGradient id="titleGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#ffffff" />
      <stop offset="28%" stop-color="#e0f7ff" />
      <stop offset="65%" stop-color="#00f0ff" />
      <stop offset="100%" stop-color="#c084fc" />
    </linearGradient>

    <!-- Brand Cyan-Violet Gradient -->
    <linearGradient id="vibeGrad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#00f0ff" />
      <stop offset="50%" stop-color="#7dd3fc" />
      <stop offset="100%" stop-color="#a855f7" />
    </linearGradient>

    <linearGradient id="badgeBorderGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#00f0ff" stop-opacity="0.85" />
      <stop offset="50%" stop-color="#38bdf8" stop-opacity="0.2" />
      <stop offset="100%" stop-color="#a855f7" stop-opacity="0.85" />
    </linearGradient>

    <linearGradient id="cardGrad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#0c1426" stop-opacity="0.8" />
      <stop offset="100%" stop-color="#050914" stop-opacity="0.92" />
    </linearGradient>

    <linearGradient id="scanlineGrad" x1="0%" y1="0%" x2="0%" y2="100%">
      <stop offset="0%" stop-color="#00f0ff" stop-opacity="0" />
      <stop offset="50%" stop-color="#00f0ff" stop-opacity="0.18" />
      <stop offset="100%" stop-color="#a855f7" stop-opacity="0" />
    </linearGradient>

    <linearGradient id="lineFadeX" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#00f0ff" stop-opacity="0" />
      <stop offset="20%" stop-color="#00f0ff" stop-opacity="0.4" />
      <stop offset="80%" stop-color="#a855f7" stop-opacity="0.4" />
      <stop offset="100%" stop-color="#a855f7" stop-opacity="0" />
    </linearGradient>

    <!-- Glow Filters -->
    <filter id="glow-heavy" x="-30%" y="-30%" width="160%" height="160%">
      <feGaussianBlur stdDeviation="6" result="blur1" />
      <feGaussianBlur stdDeviation="15" result="blur2" />
      <feMerge>
        <feMergeNode in="blur2" />
        <feMergeNode in="blur1" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>

    <filter id="glow-cyan" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="3" result="blur" />
      <feMerge>
        <feMergeNode in="blur" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>

    <filter id="glow-subtle" x="-20%" y="-20%" width="140%" height="140%">
      <feGaussianBlur in="SourceGraphic" stdDeviation="1.6" result="blur" />
      <feMerge>
        <feMergeNode in="blur" />
        <feMergeNode in="SourceGraphic" />
      </feMerge>
    </filter>

    <!-- Patterns -->
    <pattern id="cyberGrid" width="40" height="40" patternUnits="userSpaceOnUse">
      <path d="M 40 0 L 0 0 0 40" fill="none" stroke="#00f0ff" stroke-width="0.7" stroke-opacity="0.04" />
      <circle cx="40" cy="40" r="0.75" fill="#00f0ff" fill-opacity="0.2" />
    </pattern>

    <pattern id="dotMatrix" width="20" height="20" patternUnits="userSpaceOnUse">
      <circle cx="2" cy="2" r="0.5" fill="#a855f7" fill-opacity="0.15" />
    </pattern>

    <!-- Clip Paths -->
    <clipPath id="heroClip">
      <rect x="0" y="0" width="1200" height="500" rx="16" />
    </clipPath>
  </defs>

  <!-- Outer Hero Canvas -->
  <g clip-path="url(#heroClip)">
    <!-- Deep Space Background -->
    <rect width="1200" height="500" fill="url(#bgGlow)" class="anim-bg" />

    <!-- Ambient Glow Auras -->
    <g class="anim-aura">
      <circle cx="480" cy="220" r="320" fill="url(#cyanAura)" />
      <circle cx="720" cy="250" r="340" fill="url(#violetAura)" />
      <circle cx="600" cy="230" r="220" fill="url(#centerCore)" />
    </g>

    <!-- High-Tech Perspective Grid & Matrix Dots -->
    <rect width="1200" height="500" fill="url(#cyberGrid)" class="anim-grid" />
    <rect width="1200" height="500" fill="url(#dotMatrix)" class="anim-grid" />

    <!-- Subtle Moving Scanline -->
    <rect x="0" y="0" width="1200" height="45" fill="url(#scanlineGrad)" class="scanline" pointer-events="none" />

    <!-- ======================================================== -->
    <!-- ORBITAL RINGS & TECH CONCENTRIC RADAR RINGS -->
    <!-- ======================================================== -->
    <g class="anim-orbit" opacity="0.18">
      <circle cx="600" cy="225" r="175" fill="none" stroke="#00f0ff" stroke-width="0.75" stroke-dasharray="4,8" />
      <circle cx="600" cy="225" r="215" fill="none" stroke="#a855f7" stroke-width="0.75" stroke-dasharray="12,18" />
      <circle cx="775" cy="225" r="3" fill="#00f0ff" filter="url(#glow-cyan)" />
    </g>

    <!-- ======================================================== -->
    <!-- ABSTRACT FUTURISTIC DEVELOPER CONSTELLATION / SILHOUETTE -->
    <!-- ======================================================== -->
    <g class="anim-nodes" opacity="0.36">
      <!-- Constellation Mesh Lines (Developer Head & Neural Network) -->
      <g stroke="url(#vibeGrad)" stroke-width="0.8" fill="none" class="anim-constellation" transform="translate(600, 225)">
        <!-- Cranial & Silhouette Nodes -->
        <polygon points="0,-125 42,-95 72,-48 80,15 62,70 24,115 -24,115 -62,70 -80,15 -72,-48 -42,-95" stroke-opacity="0.45" stroke-dasharray="3,3" />
        <polyline points="0,-125 0,-68 42,-28 62,28 24,85 0,105 -24,85 -62,28 -42,-28 0,-68" stroke-opacity="0.4" />
        
        <!-- Neural Synapses Branching to Shoulders & Cyberspace -->
        <line x1="-125" y1="-75" x2="-42" y2="-95" stroke-opacity="0.35" />
        <line x1="125" y1="-75" x2="42" y2="-95" stroke-opacity="0.35" />
        <line x1="-175" y1="2" x2="-80" y2="15" stroke-opacity="0.35" />
        <line x1="175" y1="2" x2="80" y2="15" stroke-opacity="0.35" />
        <line x1="-145" y1="78" x2="-62" y2="70" stroke-opacity="0.35" />
        <line x1="145" y1="78" x2="62" y2="70" stroke-opacity="0.35" />
        
        <line x1="-215" y1="135" x2="-145" y2="78" stroke-opacity="0.25" stroke-dasharray="2,4" />
        <line x1="215" y1="135" x2="145" y2="78" stroke-opacity="0.25" stroke-dasharray="2,4" />
        <line x1="-95" y1="135" x2="95" y2="135" stroke-opacity="0.3" />

        <!-- Cross Neural Links -->
        <line x1="-42" y1="-28" x2="42" y2="-28" stroke-opacity="0.3" />
        <line x1="-62" y1="28" x2="62" y2="28" stroke-opacity="0.25" />
      </g>

      <!-- Glowing Neural Nodes -->
      <g fill="#00f0ff" filter="url(#glow-cyan)" transform="translate(600, 225)">
        <circle cx="0" cy="-125" r="2.8" />
        <circle cx="42" cy="-95" r="2.4" />
        <circle cx="-42" cy="-95" r="2.4" />
        <circle cx="72" cy="-48" r="2.4" />
        <circle cx="-72" cy="-48" r="2.4" />
        <circle cx="80" cy="15" r="2.8" />
        <circle cx="-80" cy="15" r="2.8" />
        <circle cx="0" cy="-68" r="2.4" />
        <circle cx="42" cy="-28" r="2" />
        <circle cx="-42" cy="-28" r="2" />
        <circle cx="0" cy="105" r="2.8" fill="#a855f7" />
        <circle cx="125" cy="-75" r="2" />
        <circle cx="-125" cy="-75" r="2" />
        <circle cx="175" cy="2" r="2" fill="#a855f7" />
        <circle cx="-175" cy="2" r="2" fill="#a855f7" />
        <circle cx="145" cy="78" r="2.4" />
        <circle cx="-145" cy="78" r="2.4" />
      </g>
    </g>

    <!-- ======================================================== -->
    <!-- FLOATING DATA PARTICLES & SPARKS -->
    <!-- ======================================================== -->
    <g filter="url(#glow-subtle)">
      <!-- Left Stream -->
      <circle cx="170" cy="370" r="1.5" fill="#00f0ff" class="p1" />
      <circle cx="250" cy="410" r="2" fill="#38bdf8" class="p2" />
      <circle cx="330" cy="340" r="1.2" fill="#a855f7" class="p3" />
      <circle cx="410" cy="390" r="2.2" fill="#00f0ff" class="p4" />
      <circle cx="500" cy="430" r="1.5" fill="#7dd3fc" class="p5" />
      <!-- Right Stream -->
      <circle cx="690" cy="380" r="1.8" fill="#c084fc" class="p2" />
      <circle cx="770" cy="420" r="2.2" fill="#00f0ff" class="p1" />
      <circle cx="860" cy="360" r="1.4" fill="#e879f9" class="p4" />
      <circle cx="950" cy="400" r="2" fill="#38bdf8" class="p3" />
      <circle cx="1030" cy="350" r="1.5" fill="#a855f7" class="p5" />
      <!-- High Zenith Constellation Points -->
      <circle cx="130" cy="130" r="1.2" fill="#00f0ff" class="p6" opacity="0.6" />
      <circle cx="210" cy="85" r="1.8" fill="#a855f7" class="p3" opacity="0.7" />
      <circle cx="1000" cy="115" r="1.5" fill="#00f0ff" class="p4" opacity="0.8" />
      <circle cx="1090" cy="155" r="2" fill="#38bdf8" class="p2" opacity="0.6" />
    </g>

    <!-- ======================================================== -->
    <!-- TOP HUD BAR & SYSTEM TELEMETRY (100% INLINE TSPAN FLOW)  -->
    <!-- ======================================================== -->
    <g class="anim-header-hud">
      <!-- Top Dividing Tech Line -->
      <line x1="30" y1="46" x2="1170" y2="46" stroke="url(#lineFadeX)" stroke-width="1" />
      
      <!-- Top Left: System Status Node (Single Inline Flow) -->
      <g transform="translate(42, 32)">
        <circle cx="0" cy="-3.5" r="3.5" fill="#00f0ff" filter="url(#glow-cyan)" />
        <circle cx="0" cy="-3.5" r="7" stroke="#00f0ff" stroke-width="0.75" fill="none" opacity="0.5" />
        <text x="16" y="0" class="font-mono" font-size="9.5" letter-spacing="1">
          <tspan fill="#00f0ff" font-weight="700">SYS.AI_CORE // ONLINE</tspan>
          <tspan fill="#334155">   |   </tspan>
          <tspan fill="#64748b">LATENCY: 12ms</tspan>
        </text>
      </g>

      <!-- Top Center: Micro Chip Tag -->
      <g transform="translate(600, 28)">
        <rect x="-90" y="-11" width="180" height="22" rx="4" fill="#0c1527" stroke="#00f0ff" stroke-width="0.75" stroke-opacity="0.4" />
        <text x="0" y="4" fill="#e2e8f0" class="font-mono" font-size="9.5" font-weight="600" letter-spacing="2" text-anchor="middle">KERNEL // V4.9.2_AI</text>
      </g>

      <!-- Top Right: Identity Telemetry (Single Inline Flow - Zero Overlap) -->
      <g transform="translate(1158, 32)">
        <text x="0" y="0" class="font-mono" font-size="9.5" text-anchor="end" letter-spacing="1">
          <tspan fill="#64748b">STATUS: </tspan>
          <tspan fill="#00f0ff" font-weight="700">BUILDING</tspan>
          <tspan fill="#a855f7">   •   </tspan>
          <tspan fill="#94a3b8" font-weight="600">VIBE_CODER.exe</tspan>
        </text>
      </g>
    </g>

    <!-- ======================================================== -->
    <!-- LEFT SIDE: MINI CYBER TERMINAL HUD -->
    <!-- ======================================================== -->
    <g class="anim-terminal" transform="translate(45, 175)">
      <!-- Terminal Glass Container -->
      <rect x="0" y="0" width="220" height="135" rx="8" fill="url(#cardGrad)" stroke="#00f0ff" stroke-width="0.75" stroke-opacity="0.25" />
      <rect x="0" y="0" width="220" height="22" rx="8" fill="#0f1b33" fill-opacity="0.7" />
      
      <!-- Terminal Header Dots -->
      <circle cx="12" cy="11" r="3" fill="#ef4444" opacity="0.8" />
      <circle cx="22" cy="11" r="3" fill="#eab308" opacity="0.8" />
      <circle cx="32" cy="11" r="3" fill="#22c55e" opacity="0.8" />
      <text x="50" y="14.5" fill="#64748b" class="font-mono" font-size="8.5" font-weight="600" letter-spacing="1">BASH // AGENT_SESSION</text>
      
      <!-- Monospace Code Execution Lines -->
      <g transform="translate(12, 40)">
        <g class="t-line-1">
          <text x="0" y="0" fill="#38bdf8" class="font-mono" font-size="9" font-weight="600">&gt; SYSTEM INITIALIZED</text>
        </g>
        <g class="t-line-2">
          <text x="0" y="18" fill="#94a3b8" class="font-mono" font-size="9">developer.identity = <tspan fill="#00f0ff">"Tahir Azeem"</tspan></text>
          <text x="0" y="34" fill="#94a3b8" class="font-mono" font-size="9">mode = <tspan fill="#a855f7">"VIBE_CODER"</tspan></text>
        </g>
        <g class="t-line-3">
          <text x="0" y="50" fill="#94a3b8" class="font-mono" font-size="9">status = <tspan fill="#22c55e">"BUILDING"</tspan></text>
          <text x="0" y="66" fill="#38bdf8" class="font-mono" font-size="9">&gt; stack.sync()<tspan class="cursor" fill="#00f0ff"> _</tspan></text>
        </g>
      </g>
      <!-- Corner Tech Accent -->
      <path d="M 215 125 L 215 130 L 210 130" fill="none" stroke="#00f0ff" stroke-width="1.5" stroke-opacity="0.6" />
    </g>

    <!-- ======================================================== -->
    <!-- RIGHT SIDE: TELEMETRY & SPECS HUD -->
    <!-- ======================================================== -->
    <g class="anim-terminal" transform="translate(935, 175)">
      <!-- Right Tech Card -->
      <rect x="0" y="0" width="220" height="135" rx="8" fill="url(#cardGrad)" stroke="#a855f7" stroke-width="0.75" stroke-opacity="0.25" />
      <rect x="0" y="0" width="220" height="22" rx="8" fill="#17122b" fill-opacity="0.7" />
      <text x="12" y="14.5" fill="#c084fc" class="font-mono" font-size="8.5" font-weight="600" letter-spacing="1">SYS_METRICS // CORE</text>
      <circle cx="206" cy="11" r="3" fill="#a855f7" filter="url(#glow-cyan)" />

      <!-- Metrics Details -->
      <g transform="translate(14, 40)">
        <text x="0" y="0" fill="#64748b" class="font-mono" font-size="8.5">NEURAL_SYNC</text>
        <text x="192" y="0" fill="#00f0ff" class="font-mono" font-size="9" font-weight="700" text-anchor="end">99.98%</text>
        <!-- Bar 1 -->
        <rect x="0" y="6" width="192" height="3" rx="1.5" fill="#1e293b" />
        <rect x="0" y="6" width="186" height="3" rx="1.5" fill="url(#vibeGrad)" />

        <text x="0" y="24" fill="#64748b" class="font-mono" font-size="8.5">FULL_STACK_FLOW</text>
        <text x="192" y="24" fill="#a855f7" class="font-mono" font-size="9" font-weight="700" text-anchor="end">OPTIMAL</text>
        <!-- Bar 2 -->
        <rect x="0" y="30" width="192" height="3" rx="1.5" fill="#1e293b" />
        <rect x="0" y="30" width="170" height="3" rx="1.5" fill="url(#vibeGrad)" />

        <text x="0" y="48" fill="#64748b" class="font-mono" font-size="8.5">AI_BUILDER_ENGINE</text>
        <text x="192" y="48" fill="#38bdf8" class="font-mono" font-size="9" font-weight="700" text-anchor="end">ONLINE</text>
        <!-- Bar 3 -->
        <rect x="0" y="54" width="192" height="3" rx="1.5" fill="#1e293b" />
        <rect x="0" y="54" width="192" height="3" rx="1.5" fill="url(#vibeGrad)" />

        <text x="0" y="72" fill="#475569" class="font-mono" font-size="8.5">THREAD_ID: <tspan fill="#94a3b8">#0x7FFE9A14</tspan></text>
      </g>
      <!-- Corner Tech Accent -->
      <path d="M 5 130 L 0 130 L 0 125" fill="none" stroke="#a855f7" stroke-width="1.5" stroke-opacity="0.6" />
    </g>

    <!-- ======================================================== -->
    <!-- CENTER STAGE: FOCAL TYPOGRAPHY & IDENTITY -->
    <!-- ======================================================== -->
    
    <!-- 1. Pre-Title Tag -->
    <g class="anim-pretitle">
      <rect x="465" y="96" width="270" height="24" rx="12" fill="#081020" stroke="url(#badgeBorderGrad)" stroke-width="1" />
      <circle cx="482" cy="108" r="2.5" fill="#00f0ff" filter="url(#glow-cyan)" />
      <text x="600" y="112" fill="#7dd3fc" class="font-mono" font-size="9.5" font-weight="700" letter-spacing="2.5" text-anchor="middle">
        ✦ DEVELOPER PROFILE ✦
      </text>
      <circle cx="718" cy="108" r="2.5" fill="#a855f7" filter="url(#glow-cyan)" />
    </g>

    <!-- 2. PRIMARY NAME: TAHIR AZEEM -->
    <g class="anim-title">
      <!-- Ambient Backglow Layer -->
      <text x="600" y="188" fill="#00f0ff" opacity="0.32" class="font-display" font-size="64" font-weight="900" letter-spacing="6" text-anchor="middle" filter="url(#glow-heavy)">
        TAHIR AZEEM
      </text>
      <!-- Main Text Surface -->
      <text x="600" y="188" fill="url(#titleGrad)" class="font-display" font-size="64" font-weight="900" letter-spacing="6" text-anchor="middle">
        TAHIR AZEEM
      </text>
    </g>

    <!-- 3. PRIMARY IDENTITY BADGE: VIBE CODER -->
    <g class="anim-badge">
      <!-- Cyber Pill Container -->
      <rect x="480" y="230" width="240" height="32" rx="6" fill="#070e1e" stroke="url(#vibeGrad)" stroke-width="1.2" filter="url(#glow-subtle)" />
      <!-- Micro HUD Crosshairs on Badge -->
      <path d="M 486 236 L 486 230 L 492 230" fill="none" stroke="#00f0ff" stroke-width="1.5" />
      <path d="M 714 256 L 714 262 L 708 262" fill="none" stroke="#a855f7" stroke-width="1.5" />
      <!-- Badge Content -->
      <text x="600" y="251.5" fill="url(#vibeGrad)" class="font-display" font-size="14.5" font-weight="800" letter-spacing="5" text-anchor="middle">
        &lt; VIBE CODER /&gt;
      </text>
    </g>

    <!-- 4. SECONDARY IDENTITY: FULL STACK DEVELOPER × AI BUILDER -->
    <g class="anim-subtitle">
      <text x="600" y="302" class="font-display" text-anchor="middle">
        <tspan fill="#f1f5f9" font-size="14.5" font-weight="700" letter-spacing="3.5">FULL STACK DEVELOPER</tspan>
        <tspan fill="#a855f7" font-size="16" font-weight="900">  ×  </tspan>
        <tspan fill="#38bdf8" font-size="14.5" font-weight="700" letter-spacing="3.5">AI BUILDER</tspan>
      </text>
    </g>

    <!-- 5. TAGLINE: "Turning ideas into real products." -->
    <g class="anim-tagline">
      <text x="600" y="342" fill="#94a3b8" class="font-display" font-size="14" font-style="italic" font-weight="400" letter-spacing="1.5" text-anchor="middle">
        “Turning ideas into real products.”
      </text>
    </g>

    <!-- 6. INTERACTIVE ACTION PILLS / QUICK LINKS -->
    <g class="anim-actions">
      <!-- GitHub Action Pill -->
      <g class="btn-hover">
        <rect x="430" y="380" width="130" height="28" rx="14" fill="#0d172e" stroke="#38bdf8" stroke-width="0.8" stroke-opacity="0.6" />
        <circle cx="452" cy="394" r="3" fill="#00f0ff" />
        <text x="500" y="398" fill="#e2e8f0" class="font-mono" font-size="10" font-weight="600" letter-spacing="1.5" text-anchor="middle">⌥ GITHUB</text>
      </g>

      <!-- Projects Action Pill -->
      <g class="btn-hover">
        <rect x="640" y="380" width="135" height="28" rx="14" fill="#140f29" stroke="#c084fc" stroke-width="0.8" stroke-opacity="0.6" />
        <circle cx="662" cy="394" r="3" fill="#a855f7" />
        <text x="712" y="398" fill="#e2e8f0" class="font-mono" font-size="10" font-weight="600" letter-spacing="1.5" text-anchor="middle">⚡ PROJECTS</text>
      </g>
    </g>

    <!-- ======================================================== -->
    <!-- BOTTOM FOOTER HUD BAR & SYSTEM METADATA -->
    <!-- ======================================================== -->
    <g class="anim-footer-hud">
      <!-- Bottom Tech Line -->
      <line x1="30" y1="458" x2="1170" y2="458" stroke="url(#lineFadeX)" stroke-width="1" />

      <!-- Bottom Left: Tech Stack Nodes -->
      <g transform="translate(42, 478)">
        <text x="0" y="0" fill="#475569" class="font-mono" font-size="9.5" letter-spacing="1.5">
          STACK: <tspan fill="#38bdf8">TypeScript</tspan> • <tspan fill="#38bdf8">React / Next.js</tspan> • <tspan fill="#a855f7">Python</tspan> • <tspan fill="#a855f7">LLMs &amp; Agents</tspan> • <tspan fill="#38bdf8">Node.js</tspan>
        </text>
      </g>

      <!-- Bottom Right: Copyright & System Core -->
      <g transform="translate(1158, 478)" text-anchor="end">
        <text x="0" y="0" fill="#475569" class="font-mono" font-size="9.5" letter-spacing="1.5">
          <tspan fill="#64748b">TAHIR_AZEEM</tspan> // <tspan fill="#00f0ff">PORTFOLIO_SYSTEM_2026</tspan> [ SEC_ALPHA ]
        </text>
      </g>
    </g>

    <!-- ======================================================== -->
    <!-- SCI-FI CORNER BRACKETS & OUTER FRAME -->
    <!-- ======================================================== -->
    <!-- Outer Border -->
    <rect x="1" y="1" width="1198" height="498" rx="15" fill="none" stroke="url(#badgeBorderGrad)" stroke-width="1" opacity="0.45" />

    <!-- High-Tech Corner Reticles -->
    <!-- Top-Left -->
    <path d="M 12 30 L 12 12 L 30 12" fill="none" stroke="#00f0ff" stroke-width="2.5" />
    <circle cx="16" cy="16" r="1.5" fill="#00f0ff" />
    <!-- Top-Right -->
    <path d="M 1188 30 L 1188 12 L 1170 12" fill="none" stroke="#a855f7" stroke-width="2.5" />
    <circle cx="1184" cy="16" r="1.5" fill="#a855f7" />
    <!-- Bottom-Left -->
    <path d="M 12 470 L 12 488 L 30 488" fill="none" stroke="#00f0ff" stroke-width="2.5" />
    <circle cx="16" cy="484" r="1.5" fill="#00f0ff" />
    <!-- Bottom-Right -->
    <path d="M 1188 470 L 1188 488 L 1170 488" fill="none" stroke="#a855f7" stroke-width="2.5" />
    <circle cx="1184" cy="484" r="1.5" fill="#a855f7" />
  </g>
</svg>
